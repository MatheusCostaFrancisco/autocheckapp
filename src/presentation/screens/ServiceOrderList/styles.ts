import {Base_Value, Small_Padding} from '../../theme/spacer';

export const SearchHeader = styled.View`
  width: 100%;
  flex-direction: row;
  padding: ${Base_Value}px ${Small_Padding}px;
  justify-content: space-between;
`;
